/**
 * 將 rem 統一設置為 10px，這樣後續如果有甚麼調整 rem 的需求，也比較好一併調整
 */
window.addEventListener('DOMContentLoaded', function () {
  // Code to be executed when the DOM is ready
  document.documentElement.style.fontSize = '10px';
});
